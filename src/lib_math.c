/* i'm writing this just because surprisingly
 * there is no implmentation of min/max function
 *
 * any other trivial-math function should totaly
 * go here
 *
 */

int max(int a, int b)
{
	return( (a > b) ? (a) : (b) );	
}

int min(int a, int b)
{
	return( (a > b) ? (b) : (a) );	
}